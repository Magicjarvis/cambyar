form(action='/lessons', method='POST')
  input(type='text', name='search_bar')#search_bar
  input(type='submit', value='Search')
each lesson in lessons
  .lesson(onclick="location.href='/lessons/"+lesson._id+"';")   
    p
      h3= lesson.title
    p= lesson.description
